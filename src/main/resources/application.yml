spring:
    datasource:
        url: jdbc:mysql://localhost:4306/board?useSSL=false&serverTimezone=Asia/Seoul
        username: root
        password: root
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
                format_sql: false
        open-in-view: false
